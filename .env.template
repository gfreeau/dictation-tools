# API Keys
# OpenAI API key - for OpenAI models or API mode transcription
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# OpenRouter API key - for Gemini and other models via OpenRouter
# Get your API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Whisper transcription mode: "local" (faster-whisper) or "api" (OpenAI API)
# Default: local
WHISPER_MODE=local

# Model to use for dictation cleanup
# OpenRouter models (recommended - faster and cheaper):
#   google/gemini-2.5-flash-lite  (default - 4x faster, ~80% cheaper, 100% eval pass)
#   google/gemini-2.0-flash-001   (fixes homophones better, 2.4x faster, ~60% cheaper, 100% eval pass)
# OpenAI models (requires OPENAI_API_KEY):
#   gpt-4o-mini                   (baseline, 100% eval pass)
#   gpt-4.1-mini                  (more expensive, 100% eval pass)
# Default: google/gemini-2.5-flash-lite
CLEANUP_MODEL=google/gemini-2.5-flash-lite

# Enable/disable GPT cleanup after transcription
# Default: true
WHISPER_CLEANUP=true

# Local Whisper model size (for WHISPER_MODE=local)
# Options:
#   base       - ~145MB, fast (~0.9s),     multilingual (recommended)
#   base.en    - ~145MB, fast (~0.9s),     English-only
#   small      - ~466MB, medium (~2s),     multilingual
#   small.en   - ~466MB, medium (~2s),     English-only
#   medium     - ~1.5GB, slow (~5-8s),     multilingual
#   medium.en  - ~1.5GB, slow (~5-8s),     English-only
# Default: base
WHISPER_MODEL_SIZE=base

# Compute type for local Whisper (for WHISPER_MODE=local)
# Options: int8, float16, float32
# Default: int8
WHISPER_COMPUTE_TYPE=int8